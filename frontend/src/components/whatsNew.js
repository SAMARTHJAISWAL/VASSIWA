import React from 'react';
import styled from 'styled-components';

const PageWrapper = styled.div`
  padding: 20px;
`;

const WhatsNew = () => {
  return (
    <PageWrapper>
      <h2>Events</h2>
      <p>All the events will be listed here.</p>
    </PageWrapper>
  );
};

export default WhatsNew;
